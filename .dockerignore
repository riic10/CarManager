# Keep the Docker build context small — copy only what the build needs (pom.xml, src).
target/
.git/
.github/
.vscode/
.claude/
docs/
data/
*.md
HELP.md

# The frontend is built/deployed separately (Vercel); keep it (and its
# node_modules) out of the backend image build context.
frontend/
